# models/ — machines, not measurements

Every script prints ASSUMED at the bottom. Defaults are arguments.

| file | object | do not read as |
|---|---|---|
| `budget.py` | mechanical WOB at rest | 426 as cost of breathing |
| `chain.py` | heat + water + WOB + cardiac share | 54.7 as measured; 426 in range |
| `bohr.py` | hypocapnic alkalosis → extraction | finger SpO2 as tissue unloading |
| `swing.py` | pH both ways on one event | 49.6 T as the kit stock; Sv change as the whole event |
| `fetus.py` | two curves, ovine transmit ratio | 15% as human; “sats track PO2”; double Bohr as computed (it is named, not calculated) |

`clocks.py` is **not in this packet**. Transport delay remains a load-bearing guess until that file is on a hashed tree.
